<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lembe.user.mapper.SocialAccountMapper">

    <resultMap id="SocialAccountResultMap" type="com.lembe.user.domain.SocialAccount">
        <id     property="socialAccountSeq" column="social_account_seq"/>
        <result property="userSeq"          column="user_seq"/>
        <result property="provider"         column="provider"/>
        <result property="providerId"       column="provider_id"/>
        <result property="email"            column="email"/>
        <result property="createdAt"        column="created_at"/>
        <result property="updatedAt"        column="updated_at"/>
    </resultMap>

    <select id="findByProviderAndProviderId" resultMap="SocialAccountResultMap">
        SELECT *
        FROM tb_social_account
        WHERE provider    = #{provider}
          AND provider_id = #{providerId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="socialAccountSeq">
        INSERT INTO tb_social_account (user_seq, provider, provider_id, email)
        VALUES (#{userSeq}, #{provider}, #{providerId}, #{email})
    </insert>

</mapper>
